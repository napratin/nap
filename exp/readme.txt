Experiments meant to be run with nap agents and applications.

Most experiments should have their own contained data/output directories with proper gitignores. Experiment scripts are usually to be run from their root folder (not nap/).
